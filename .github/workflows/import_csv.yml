name: Import New Data

on:
  workflow_dispatch:
    inputs:
      csv_content:
        description: 'Dán nội dung text của file CSV vào đây'
        required: false
        type: string
      csv_url:
        description: 'Hoặc nhập link URL công khai tới file CSV để tải về'
        required: false
        type: string

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Restore Cached Stock Data
        uses: actions/cache@v3
        with:
          path: |
            data_storage/
            config.json
          key: stock-data-import-${{ github.run_id }}
          restore-keys: |
            stock-data-

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install gdown

      - name: Fetch Data from Google Drive
        run: |
          mkdir -p gdrive_data
          if gdown --folder https://drive.google.com/drive/folders/1zxsHmgb1LAhD0I8hR9BJnsj5IVwDSbli -O gdrive_data/; then
            echo "✅ Google Drive folder fetch completed."
          else
            echo "⚠️ Lỗi tải hoặc link folder bị khóa."
          fi

      - name: Prepare Input Data
        env:
          CSV_TEXT: ${{ github.event.inputs.csv_content }}
          CSV_URL: ${{ github.event.inputs.csv_url }}
        run: |
          if [ -n "$CSV_TEXT" ]; then
            echo "$CSV_TEXT" > import_data.csv
            echo "✅ Đã lưu nội dung CSV dán từ giao diện vào import_data.csv"
          elif [ -n "$CSV_URL" ]; then
            echo "📥 Đang tải CSV từ link URL: $CSV_URL"
            curl -L -o import_data.csv "$CSV_URL"
            echo "✅ Đã tải file CSV từ URL vào import_data.csv"
          elif [ -d "gdrive_data" ] && [ "$(ls -A gdrive_data/*.csv 2>/dev/null)" ]; then
            echo "✅ Phát hiện file CSV trong thư mục Google Drive. Đang chép sang import_data/"
            mkdir -p import_data
            cp gdrive_data/*.csv import_data/
          else
            echo "ℹ️ Không có dữ liệu từ Google Drive (hoặc Drive trống). Sẽ sử dụng dữ liệu mặc định từ Git."
          fi

      - name: Run Import Script
        run: |
          if [ -f "import_data.csv" ]; then
              echo "=== PHÁT HIỆN FILE import_data.csv. BẮT ĐẦU NẠP DỮ LIỆU BAN ĐẦU ==="
              python run_headless_update.py --import-csv import_data.csv
          elif [ -d "import_data" ] && [ "$(ls -A import_data/*.csv 2>/dev/null)" ]; then
              echo "=== PHÁT HIỆN THƯ MỤC import_data/ CHỨA FILE CSV. BẮT ĐẦU NẠP DỮ LIỆU BAN ĐẦU ==="
              python run_headless_update.py --import-csv import_data/
          else
              echo "❌ Không tìm thấy thư mục import_data/ hoặc file import_data.csv để nạp!"
              exit 1
          fi

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Stage only the Output/ directory (data_storage/ and config.json are handled by GitHub cache)
          git add Output/
          
          if ! git diff --cached --quiet; then
            git commit -m "Import stock data from CSV: $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          else
            echo "No changes in stock data. Skipping push."
          fi
